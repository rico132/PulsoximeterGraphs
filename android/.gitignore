*.iml
.gradle/
.kotlin/
/local.properties
.idea/
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
local.properties
app/build/
# Room's exported schema history (app/schemas/) is intentionally NOT ignored — it's
# meant to be committed and used for migration testing as the schema evolves.
